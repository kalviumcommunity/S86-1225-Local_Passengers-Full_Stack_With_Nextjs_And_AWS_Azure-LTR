meta {
  name: Invalid Token Test
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/admin
  body: none
  auth: bearer
}

auth:bearer {
  token: invalid_jwt_token_xyz123
}

docs {
  ## Test: Invalid JWT Token
  
  **Expected Result:** 403 Forbidden
  
  This request uses an invalid JWT token.
  The middleware should verify the token signature and reject it:
  
  ```json
  {
    "success": false,
    "message": "Invalid or expired token. Please login again.",
    "errorCode": "AUTH_TOKEN_INVALID"
  }
  ```
  
  This demonstrates token validation security.
}
