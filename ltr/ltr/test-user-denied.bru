meta {
  name: User Access Denied Test
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/admin
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

docs {
  ## Test: User Role Accessing Admin Route
  
  **Expected Result:** 403 Forbidden
  
  This request uses a USER role token to access an admin-only route.
  The middleware should block access and return:
  
  ```json
  {
    "success": false,
    "message": "Access denied. Admin privileges required.",
    "errorCode": "FORBIDDEN_ACCESS"
  }
  ```
  
  This demonstrates the Role-Based Access Control (RBAC) in action.
}
