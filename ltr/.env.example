.env.example
# Server-side variables
# Local Development (Docker)
DATABASE_URL=postgres://postgres:password@db:5432/mydb

# Cloud Database Configuration
# AWS RDS PostgreSQL: postgresql://username:password@rds-endpoint.region.rds.amazonaws.com:5432/dbname
# Azure PostgreSQL: postgresql://username%40servername:password@servername.postgres.database.azure.com:5432/dbname?sslmode=require
# Example: DATABASE_URL=postgresql://admin:YourStrongPassword@nextjs-db.c1a2b3c4d5e6.ap-south-1.rds.amazonaws.com:5432/localpassengers

# JWT Authentication Secrets
# IMPORTANT: Generate strong, random secrets for production
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Access Token Secret - Used to sign short-lived access tokens (15 min)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Refresh Token Secret - Used to sign long-lived refresh tokens (7 days)
# MUST be different from JWT_SECRET for security
JWT_REFRESH_SECRET=your-super-secret-refresh-key-different-from-jwt-secret-min-32-chars

ADMIN_EMAIL=admin@example.com

# AWS S3 Configuration (for File Upload Feature)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=your-s3-bucket-name

# Legacy Cloud Storage (if not using AWS)
CLOUD_STORAGE_KEY=
CLOUD_STORAGE_SECRET=

# Client-side variables
NEXT_PUBLIC_API_BASE_URL=http://localhost:5174
NEXT_PUBLIC_APP_NAME=Local Passengers
NEXT_PUBLIC_VERSION=1.0.0